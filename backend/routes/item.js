const express = require("express");
const router = express.Router();

const { fetchItem } = require("@/backend/controllers/ItemController");

// router.get("/category", fetchCategory);

module.exports = router;
